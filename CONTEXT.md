# CONTEXT

このプロジェクトで使う言葉の辞書。**実装の詳細・設計判断・段取りは書かない**
（それらは `PLAN.md` と `docs/adr/` にある）。用語が確定するたびにここへ追記する。

## ミノ (Mino)

盤面を落ちてくる、4つのセルからなるブロック。7種類ある（I, O, T, S, Z, J, L）。
「テトロミノ」の略。**「ブロック」「ピース」とは呼ばない**——「ブロック」は
積み上がった後のセルと紛らわしいため。

## アクティブミノ (Active Mino)

いま操作対象になっているミノ。盤面上にちょうど1つ存在する。
ロックされた瞬間にアクティブミノではなくなり、盤面のセルに変わる。

## 回転状態 (Rotation State)

アクティブミノがどの向きを向いているか。初期・右回り1回・2回・3回の 4 つで、
慣例に従い **0 / R / 2 / L** と呼ぶ。ミノの種類と回転状態の組がひとつの形を決める。
「角度」とは呼ばない——回転は連続的な角度ではなく 4 つの離散した状態でしかない。
形は 4 状態それぞれについて個別に定まっているものであって、実行時に座標を
回して求めるものではない。キックテーブルの引き当ても
「どの回転状態からどの回転状態へ」で行う。

## 盤面 (Board)

ミノが積み上がる 10 列 × 20 行のグリッド。**盤面が保持するのは
ロック済みのセルだけ**で、アクティブミノは盤面の一部ではない。
両者を重ね合わせた結果が画面に見えるものになる。

## セル (Cell)

盤面の 1 マス。空か、いずれかのミノ由来の色を持つ。

## ロック (Lock)

アクティブミノがそれ以上落下できなくなり、盤面のセルとして確定すること。
接地とロックは**同じ瞬間ではない**（→ ロックディレイ）。

## ロックディレイ (Lock Delay)

接地してからロックされるまでの猶予時間。この間はまだ動かせるため、
狭い隙間に横から滑り込ませられる。これが無いと着地の瞬間に確定してしまう。

## ライン消去 (Line Clear)

1 行がセルで埋まったとき、その行を取り除き、上の行を落とすこと。

## ゴースト (Ghost)

アクティブミノが今ハードドロップしたら着地する位置を、盤面上に薄く示す表示。
ゴーストは盤面のセルではなく、描画上の存在でしかない。

## ハードドロップ / ソフトドロップ

**ハードドロップ**は一気に落として即座にロックする操作。
**ソフトドロップ**は落下を加速するだけで、ロックディレイは通常どおり働く。

## ホールド (Hold)

アクティブミノを 1 つだけ退避し、後で取り出す機能。
1 回ホールドしたら、次にミノがロックされるまで再ホールドはできない。

## 7バッグ (7-Bag)

次に出るミノの決め方。7 種を 1 セットとしてシャッフルし、使い切ったら
次のセットを作る。完全ランダムだと同じミノが何度も連続して理不尽になるため。

## ネクスト (Next)

これから出てくるミノの予告表示。

## キックテーブル (Kick Table)

壁際などでミノを回転させたときに、めり込みを避けてズラす候補位置の一覧。
「ミノの種類 × 回転の向き」ごとに、試す順番の決まった座標の列を持つ。
回転処理そのものはこの表を順に試すだけで、**表の中身が回転の性格を決める**。

## 抽選 (Draw)

次のアクティブミノとして何を出すかを 1 つ決めること。**抽選の方法はコアの外にある**——
コアは「次の 1 つ」を求めるだけで、それが完全ランダムなのか 7バッグなのかを知らない。
7バッグの導入は、コアではなく抽選の中身の差し替えとして行う。

## フレーム (Frame)

コアが 1 描画分として吐き出す、ANSI エスケープを含んだ文字列。
CLI 版はこれを標準出力へ書き、ブラウザ版は xterm.js へ流し込む。
**フレームがコアと表示の唯一の境界**である。

## 入力 (Input)

「左へ移動」「時計回りに回転」のように、ゲームに対して意味を持つ操作ひとつ。
矢印キーでもタッチボタンでも、押されたものは入力に変換されてからコアへ渡る。
**コアはキーもボタンも知らず、入力しか知らない**。フレームが出力側の唯一の境界で
あるのと対称に、入力がコア側から見た操作の唯一の境界になる。

## ドライバ (Driver)

コアを動かす環境側のプログラム。CLI 版とブラウザ版の 2 つがある。
時間を計ってコアを進め、キー入力を入力へ変換して渡し、フレームを画面へ流す。
**ゲームのルールはドライバには一切無い**。
